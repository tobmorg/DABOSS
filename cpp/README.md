# The Debiasing Algorithm

This directory contains the scripts for the debiasing algorithms. The central scripts are located in the *src/* subdirectory. The functionality of each script can be gleaned from its comments. If you want to change the input parameters of the debiasing procedure, you can do that in the *src/run_debiasing.cpp* file.